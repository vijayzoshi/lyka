package com.example.myapplication.adapter

class PersonalchatsAdapter(){
/*
//class PersonalchatsAdapter(val context : Context, val datalist : ArrayList<PersonalchatsModel>) : RecyclerView.Adapter<PersonalchatsAdapter.MyViewholder>() {


    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): PersonalchatsAdapter.MyViewholder {
        var view : View = LayoutInflater.from(parent.context).inflate(R.layout.rv_sentmsg, parent, false)
        return PersonalchatsAdapter.MyViewholder(view)
    }

    override fun onBindViewHolder(holder: PersonalchatsAdapter.MyViewholder, position: Int) {
    holder.sendMsg.text = datalist.get(position).sendmsg
        holder.sendTime.text = datalist.get(position).sendtime

    }

    override fun getItemCount(): Int {
         return datalist.size
    }

    class SentViewholder( itemview : View) : RecyclerView.ViewHolder(itemview) {

        val sendMsg = itemview.findViewById<TextView>(R.id.tv_receivemsg)
        val sendTime = itemview.findViewById<TextView>(R.id.tv_receivetime)
    }

    class ReceiveViewholder(itemview : View) : RecyclerView.ViewHolder(itemview){
        val recieveMsg = itemview.findViewById<TextView>(R.id.tv_receivemsg)
        val receiveTime = itemview.findViewById<TextView>(R.id.tv_receivetime)
    }
*/
}

